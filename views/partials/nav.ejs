<%
const currentPath = (typeof path === 'string') ? path : '';
const activeRole = (user && user.role) ? user.role : '';
const isDev = Boolean(typeof isDeveloper !== 'undefined' && isDeveloper);

const roleLinks = {
  admin: [['/admin','Dashboard'],['/admin/students','Students'],['/admin/mentors','Mentors'],
          ['/admin/slots','Slots'],['/admin/interviews','Interviews'],['/admin/reports','Reports'],
          ['/admin/audit','Audit']],
  mentor: [['/mentor','My interviews']],
  student: [['/student','Dashboard'],['/student/mentors','Mentors'],['/student/slots?type=technical','Book slots'],['/student/results','My results']],
  developer: [
    ['/admin', 'Admin Dashboard'],
    ['/mentor', 'Mentor Desk'],
    ['/student', 'Student View'],
    ['/admin/slots', 'Slots'],
    ['/admin/interviews', 'Interviews'],
    ['/admin/reports', 'Reports'],
  ],
};

const links = (isDev && activeRole === 'developer')
  ? roleLinks.developer
  : (roleLinks[activeRole] || roleLinks[user ? user.role : ''] || []);

const userName = (user && user.name) ? String(user.name).trim() : '';
const initials = userName ? userName.split(/\s+/).map(w => w[0] || '').slice(0, 2).join('').toUpperCase() : '?';
%>
<header class="topbar">
  <div class="topbar-in">
    <a href="/" class="brand">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
        <rect width="24" height="24" rx="6" fill="#17171c"/>
        <path d="M7 12L10.5 15.5L17 8.5" stroke="#ffffff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span>Konfident</span>
      <span class="brand-tag faint">SYSTEM</span>
    </a>
    <nav class="nav">
      <% links.forEach(function(l){ %>
        <a href="<%= l[0] %>" class="<%= currentPath === l[0].split('?')[0] ? 'on' : '' %>"><%= l[1] %></a>
      <% }); %>
    </nav>
    <div class="who">
      <a href="/profile" class="who-link" title="My profile and password settings">
        <span class="avatar"><%= initials %></span>
        <span class="name">
          <strong><%= user.name %></strong>
          <% if (isDev) { %>
            <span class="pill role-pill" style="background:#e0e7ff;color:#3730a3;border:1px solid #c7d2fe;font-weight:700">DEV</span>
          <% } else { %>
            <span class="pill role-pill p-<%= activeRole === 'admin' ? 'brand' : (activeRole === 'mentor' ? 'hr' : 'technical') %>"><%= activeRole %></span>
          <% } %>
        </span>
      </a>
      <% if (typeof isDualRole !== 'undefined' && isDualRole && !isDev) { %>
        <form method="post" action="/switch-role" style="margin:0;display:inline">
          <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
          <input type="hidden" name="targetRole" value="<%= activeRole === 'admin' ? 'mentor' : 'admin' %>">
          <button type="submit" class="btn sm" style="background:var(--surface-alt);border:1px solid var(--line-medium);font-weight:600;font-size:12px;padding:5px 10px;cursor:pointer" title="Switch to <%= activeRole === 'admin' ? 'Mentor' : 'Admin' %> mode">
            Switch to <%= activeRole === 'admin' ? 'Mentor' : 'Admin' %> →
          </button>
        </form>
      <% } %>
      <form method="post" action="/logout" style="margin:0">
        <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
        <button class="btn sm who-action">Sign out</button>
      </form>
    </div>
  </div>
</header>
<main class="wrap">
<% if (flash) { %><div class="flash <%= flash.type %>"><span><%= flash.msg %></span><button type="button" class="flash-close-btn" onclick="this.parentElement.remove()" aria-label="Dismiss">&times;</button></div><% } %>
